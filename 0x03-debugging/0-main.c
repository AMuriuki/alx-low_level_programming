#include "main.h"

/**
 * main - test the function that print if integer is positive or negative
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
